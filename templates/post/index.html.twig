{% extends 'base.html.twig' %}

{% block title %}Dernières publications{% endblock %}

{% block body %}
<div>
    <h1>Dernières publications</h1>
    <ul>
        {% for post in posts %}
            {# Attention, fonctionne comme un foreach en PHP #}
            <li>{{ post.title }}</li>
        {% endfor %}
    </ul>
    <nav>
        <h2>Pages</h2>
        <ul>
            <li><a href="{{ path('app_post_index', {page: 1}) }}">1</a></li>
        </ul>
    </nav>
</div>
{% endblock %}
